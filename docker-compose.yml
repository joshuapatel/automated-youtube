services:
  callback:
    container_name: callback
    build:
      context: ./callback
      target: callback-yt-prod
    volumes:
      - "media:/yt-media"
    ports:
      - "80:80"
    stop_signal: SIGINT

volumes:
  media: